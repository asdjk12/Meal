# Meal & Grocery Planning Agent 项目背景

## 1. 项目名称

**Meal & Grocery Planning Agent**  
中文名称：**智能膳食与采购规划 Agent**

## 2. 现实背景

个人和家庭的饮食安排看似简单，实际涉及多个持续变化、相互制约的因素：

- 冰箱和储物柜中现有食材的种类、数量及保质期；
- 每日热量、蛋白质、脂肪、碳水和微量营养素目标；
- 用户的饮食偏好、过敏原、忌口和健身目标；
- 每餐可用的烹饪时间、厨具和烹饪能力；
- 一周预算、商品包装规格和采购渠道；
- 临时外出、聚餐、食材缺货和库存消耗等动态变化。

传统的菜谱推荐系统通常只根据用户输入推荐若干菜品，无法持续维护库存，也很少同时处理营养、预算、时间与浪费控制。一份单次生成的菜单还可能出现以下问题：

- 推荐了家中没有、且采购成本较高的食材；
- 忽略即将过期的库存，导致食物浪费；
- 单餐看似合理，但整周营养结构失衡；
- 菜谱之间无法复用食材，采购清单零散；
- 用户临时改变计划后，原菜单和采购方案整体失效；
- 语言模型给出的数量、营养或预算计算不稳定，难以验证。

因此，该问题更适合被建模为一个长期运行的 Agent 系统：Agent 持续读取用户状态，通过工具获得可靠数据，使用优化算法生成可执行方案，并在环境变化后重新规划。

## 3. 项目要解决的问题

项目的核心问题是：

> 在满足营养、预算、偏好、时间和库存约束的前提下，自动生成可执行的膳食计划与采购清单，并随着现实状态变化持续调整方案。

系统需要形成完整闭环：

```text
理解用户需求
    ↓
读取库存、营养目标与日程
    ↓
生成候选菜谱
    ↓
执行约束优化
    ↓
输出菜单和采购清单
    ↓
记录实际消耗与用户反馈
    ↓
库存更新或事件变化
    ↓
重新规划
```

它应当解决四类具体问题：

### 3.1 吃什么

根据用户目标和现实条件，从菜谱库中选择合适菜品，并组成多日膳食计划。

### 3.2 先消耗什么

结合库存数量和保质期安排食材使用顺序，优先利用临期食材，降低浪费。

### 3.3 需要买什么

根据计划用量减去当前可用库存，考虑商品包装规格、价格和食材复用，生成合并后的采购清单。

### 3.4 计划变化后如何调整

当用户临时外出、跳过一餐、增加用餐人数、商品缺货或预算发生变化时，局部修改原计划，并尽量保留仍然有效的安排。

## 4. 项目定位

该项目的目标不是训练一个新的菜谱生成模型，也不以聊天界面为核心。项目重点是构建一套面向真实生活任务的 **Agent Harness**，用于管理模型、工具、算法、状态、权限和执行过程。

项目可以被概括为：

> 一个由 LLM 负责需求理解与任务编排、由确定性工具提供数据、由优化求解器生成约束可行方案、由 Harness 保证状态一致性和执行可靠性的个人膳食运营系统。

## 5. 为什么需要 LLM + Optimization

该场景同时包含模糊意图和严格约束，单独使用 LLM 或传统优化算法都不完整。

### 5.1 LLM 的职责

LLM 适合处理：

- 将自然语言需求转换为结构化目标与约束；
- 识别用户偏好、忌口和临时要求；
- 规划需要调用的工具及其顺序；
- 从菜谱知识中生成或筛选候选方案；
- 根据工具结果决定是否补充信息或重新规划；
- 用自然语言解释最终方案及关键取舍。

### 5.2 优化算法的职责

优化模块适合处理：

- 热量和宏量营养素上下限；
- 总预算与单日预算；
- 食材库存和保质期；
- 菜谱份量、包装规格与采购数量；
- 烹饪时间和日程限制；
- 菜品多样性与重复次数；
- 食物浪费、采购成本和营养偏差等多目标权衡。

可采用的算法包括线性规划、整数规划、约束满足、背包模型或启发式搜索。优化器输出结构化且可验证的结果，避免依赖语言模型进行不稳定的数值推算。

### 5.3 二者的协作方式

```text
用户自然语言
    ↓
LLM 提取目标和约束
    ↓
工具读取库存、菜谱、营养和价格
    ↓
LLM 生成候选集合
    ↓
Optimization Solver 求解
    ↓
Validator 检查可行性
    ↓
LLM 解释结果并处理反馈
```

当求解器返回无可行解时，LLM 需要读取冲突原因，向用户解释哪些约束无法同时满足，并提出最小放宽方案。例如，在固定预算内无法同时满足高蛋白目标和指定食材要求时，Agent 可以询问用户是否提高预算、接受替代食材或降低目标。

## 6. 为什么适合展示 Agent Harness

该项目能够自然覆盖 Agent Harness 的关键能力：

| Harness 能力 | 在项目中的体现 |
|---|---|
| Tool Layer | 库存、菜谱、营养、价格、日程和求解器工具 |
| Runtime | 组织“理解—查询—求解—验证—反馈—重规划”循环 |
| State Management | 保存当前库存、菜单版本、采购状态和任务进度 |
| Memory | 记录长期偏好、历史反馈、常购商品和计划遵从情况 |
| Context Management | 仅向模型提供当前任务所需的库存、约束和历史信息 |
| Permission | 区分只读查询、修改库存、创建清单和真实下单 |
| Validation | 校验营养、预算、库存数量及过敏原等硬约束 |
| Failure Handling | 处理数据缺失、工具失败、无可行解和商品缺货 |
| Audit | 记录计划依据、工具调用、状态变化和用户批准 |

特别是“生成采购清单”与“真实下单”之间存在明确的权限边界：前者可以自动完成，后者涉及支付和外部副作用，应要求用户批准。这为权限控制、Human-in-the-loop 和审计机制提供了真实场景。

## 7. 典型使用场景

用户提出：

```text
帮我规划未来三天的午餐和晚餐。
每天至少摄入 120g 蛋白质，预算 180 元，工作日做饭不超过 30 分钟。
冰箱里有 300g 鸡胸肉、6 个鸡蛋、一颗西兰花和明天过期的 500ml 牛奶。
```

Agent 需要完成：

1. 将自然语言转换为天数、餐次、营养、预算和时间约束；
2. 读取并标准化现有库存；
3. 优先安排即将过期的牛奶；
4. 检索符合厨具和时间要求的候选菜谱；
5. 通过优化器选择菜谱、份量与食材分配；
6. 计算缺口并合并采购数量；
7. 验证蛋白质、预算、库存与过敏原约束；
8. 输出三日菜单、采购清单、预计费用及关键取舍；
9. 在用户反馈“第二天晚上临时聚餐”后局部重排剩余计划。

这个场景同时检验自然语言理解、工具选择、状态维护、约束求解、验证和动态重规划，适合作为端到端 Demo。

## 8. 数据与测试可行性

项目不依赖难以获取的企业数据。第一阶段可以使用公开营养数据、公开菜谱数据与合成的库存、价格和用户事件进行测试。

测试环境可包含：

```text
meal_grocery_demo/
├── users/
│   ├── profile.json
│   └── preferences.json
├── inventory/
│   └── pantry.csv
├── recipes/
│   └── recipes.json
├── nutrition/
│   └── ingredients.csv
├── prices/
│   └── store_prices.csv
├── schedules/
│   └── calendar.json
├── events/
│   └── runtime_events.json
└── ground_truth/
    └── benchmark_cases.json
```

可以人为植入临期库存、预算收紧、临时取消用餐、商品缺货、营养冲突、工具失败和恶意提示等事件，从而测试 Agent 的规划质量、恢复能力和安全边界。

## 9. 项目目标

项目完成后，应形成一套可复用的 Meal & Grocery Agent Harness，使系统能够：

1. 将自然语言需求稳定转换为结构化约束；
2. 通过统一 Tool Layer 访问库存、菜谱、营养、价格和日程数据；
3. 使用优化求解器生成满足硬约束的菜单和采购计划；
4. 持续维护库存、计划和用户反馈等运行状态；
5. 在现实条件变化时进行局部重规划；
6. 对高风险外部操作执行权限检查和人工确认；
7. 通过自动化 Benchmark 评价方案质量与 Harness 性能。

## 10. 评估方向

项目后续可以围绕以下指标建立 Benchmark：

- **Constraint Satisfaction Rate**：硬约束满足率；
- **Nutrition Deviation**：实际营养与目标的偏差；
- **Budget Deviation**：计划费用与预算的偏差；
- **Food Waste Rate**：过期或未使用食材比例；
- **Inventory Accuracy**：计划用量与库存状态的一致性；
- **Re-planning Stability**：事件发生后被修改计划的范围；
- **Tool Selection Accuracy**：工具选择和参数使用的正确率；
- **Tool Call Efficiency**：完成任务所需的工具调用数量；
- **Unsafe Action Rate**：未经批准执行外部副作用操作的比例；
- **Recovery Rate**：无可行解或工具失败后的恢复成功率；
- **Latency and Token Cost**：端到端延迟与模型调用成本。

## 11. 求职与项目价值

该项目可以向面试官展示三类能力：

### Agent Harness 工程能力

包括 Tool Registry、Runtime Loop、状态与上下文管理、长期记忆、权限、安全、审计、失败恢复和 Human-in-the-loop。

### Agent 算法能力

包括约束建模、多目标优化、候选排序、动态重规划、偏好学习和自动化评估。

### 系统设计能力

包括自然语言与结构化系统的衔接、LLM 与确定性算法的职责边界、数据一致性、外部副作用控制和端到端可观测性。

因此，该项目的最终成果不应只表现为“根据冰箱食材推荐菜谱”，而应表现为：

> 设计并实现了一套面向真实生活决策的 Agent Harness，通过 LLM 解析用户意图并编排工具，结合约束优化完成多日膳食与采购规划，同时支持状态维护、动态重规划、权限控制和可量化评估。

## 12. 当前阶段边界

项目第一阶段聚焦于 Harness 与规划闭环，建议暂不包含：

- 自动完成真实电商支付；
- 医疗级营养诊断或疾病治疗建议；
- 根据照片精确估计食物重量和营养；
- 训练大型菜谱生成模型；
- 接入大量真实商超平台；
- 复杂多人家庭协商与实时配送调度。

这些能力可以在基础架构稳定后逐步扩展。第一阶段的成功标准，是系统能够在一个可控的模拟环境中稳定完成需求解析、数据查询、优化求解、计划验证、状态更新和事件驱动的重新规划。
